1. Add Profile tab
2. Add loader for signing in/up(maybe verification) and show successful, then afterwards navigate to a user page OR show incorrect password/username
3. Show error message depending on error eg. token expired - sign up again


x. add recipes and suggested food for vitamin
x2. handle sign-in/up validation eg. min password, max name, email/name already exists
x3. replace ALL localhost urls
x4. go through all comments - especially on frontend and change logs to messages to user - make separate branch for production (keep console.log and other stuff) and deployment
=====
save user data, extend current user model to keep age, gender and so on